import React, { useState, useRef, useEffect } from "react";
import { useNavigate } from 'react-router-dom';

const LoginForm: React.FC = () => {

  
  return (
    <div className="flex flex-col w-[40%] m-auto border-2 border-gray-200 rounded-2xl px-4 py-7 sm:px-8 md:px-12">
      <div className="text-xl font-medium mb-10 self-center">로그인</div>
      
    </div>
  );
};

export default LoginForm;
